@namespace RootServer.Shared._Core

<Tab Name="@TabName" Clicked="@TabClicked" Background="@BackgroundColor">
    <Tooltip Text="@Tooltip">
        <Icon Name="@IconsName" TextColor="@IconColor" />
    </Tooltip>
</Tab>